setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - cd server && yarn prisma migrate deploy
run:
  web: echo "${GCP_CREDENTIALS}" > "${GOOGLE_APPLICATION_CREDENTIALS}" && cd server && yarn start:prod
